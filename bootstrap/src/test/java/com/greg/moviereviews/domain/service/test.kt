package com.greg.moviereviews.domain.service

import arrow.core.Option


class test {


    fun getPerson(firstName : String, lastName : String, num : String) : Person {
        for (
            fn <- Option(firstName)
        )
    }
}